# Fixtures de preprocesado

Los archivos binarios se generan en tiempo de prueba para evitar almacenarlos
en el repositorio. El fichero `noisy_staff.pgm` es una imagen en formato PGM
ASCII (texto) que sirve para validar la binarización. Los PDFs de ejemplo se
crean dinámicamente en los tests utilizando Pillow.
